<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.entunix_jimenez.firebaseapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Authentication">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainMenu" />
        <activity android:name=".SendData" />
        <activity android:name=".SingleTapProfile" />
        <activity android:name=".Register" />
        <activity android:name=".ViewProfile" />
        <activity android:name=".LongPressProfile" />
        <activity android:name=".HorizontalSwipeProfile" />
        <activity android:name=".DoubleTapProfile" />
        <activity android:name=".VerticalSwipeProfile" />
        <activity android:name=".ScrollProfile"></activity>
    </application>

</manifest>